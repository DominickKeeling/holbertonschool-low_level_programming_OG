#include "main.h"
/**
 * mul - write a fuction that writes multiplies two integers
 *
 * @a: character to check
 * @b: character to check
 *
 * Return: something
 */
int mul(int a, int b)
{
	return (a * b);
}
